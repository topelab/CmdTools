<Project>
  <PropertyGroup>
    <!-- Configuraciones comunes para todos los proyectos -->
    <LangVersion>latest</LangVersion>
    <Nullable>disable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <!-- Asegura que se usen referencias de paquete en proyectos SDK-style -->
    <RestoreProjectStyle>PackageReference</RestoreProjectStyle>
    <!-- Versiones de proyectos -->
    <ProjectExtensionVersion>1.2.7</ProjectExtensionVersion>
    <ToolsVersion>1.1.38</ToolsVersion>
    <AvaloniaProjectRelations>1.1.7</AvaloniaProjectRelations>
  </PropertyGroup>
</Project>